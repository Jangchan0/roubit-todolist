// 로그인

interface Login {
    email: string;
    password: string;
}

interface signUp {
    email: string;
    fullName: string;
    Username: string;
    password: string;
}

interface TodoList {
    id: number;
    title: string;
    completed: boolean;
}
